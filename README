Launch node with `-txindex=1 -rest`.
Configure environment in `UtxoAnalyzer.py` with the appropriate parameters.
Add appropriate script handler for bitcoin or related coin.
Create virtual environment `python3 -m venv .venv`, activate `source .venv/bin/activate`, and install `python3 -m pip install -r requirements.txt`.
Run `UtxoAnalyzer.py`.

TODO
command line args
save block height to build
